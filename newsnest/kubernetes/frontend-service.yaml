apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: newsnest
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: newsnest
    component: frontend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
