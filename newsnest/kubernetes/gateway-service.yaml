apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: newsnest
    component: api-gateway
spec:
  type: ClusterIP
  selector:
    app: newsnest
    component: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
